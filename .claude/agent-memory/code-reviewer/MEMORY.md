# Code Reviewer 에이전트 메모리

## 이 프로젝트에서 반복되는 이슈

1. **unused import → 빌드 실패** (가장 빈번)
2. **mockData 변경 후 DATA_VERSION 미증가** → 배포해도 반영 안됨
3. **flex 컨테이너 min-w-0 누락** → 모바일 overflow
4. **asset() 미사용** → 빌드 후 이미지 깨짐
5. **용어 혼용** (회원/멤버, 승인/확인)

## UX 텍스트 검증 기준

프로젝트 오너의 UX 수준이 높으므로 다음을 엄격히 검증:
- 반복 조회 화면 → 정보가 컴팩트한가?
- 최초 인지 화면 → 단계적 노출이 적용되었는가?
- 용어 표준 준수 여부 (ux-writer 메모리의 용어 표준 참조)

## 리뷰 우선순위

1. 빌드 깨지는 이슈 (심각)
2. 데이터 정합성 (심각)
3. UX 텍스트 불일치 (경고)
4. CSS 패턴 위반 (경고)
5. 코드 가독성 (제안)

## 피드백 이력

(세션에서 피드백을 받을 때마다 여기에 추가)
