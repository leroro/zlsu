# 즐수팀 운영 시스템 개발 문서

## 📋 프로젝트 개요

### 프로젝트명
즐수팀 운영 시스템 (SwimTeam Management System)

### 목적
- 수영 모임의 회원 가입, 상태 변경, 정원 관리를 자동화
- 총무 부재 시에도 운영 가능한 최소 자동화 시스템 구축
- 모임 운영 규칙(회칙)의 투명한 공개 및 관리

### 배경
- 현재 인원: 단톡방 16명, 실제 활동 12명
- 신규 가입 희망자 2명 발생
- 현재 운영 방식: 카카오톡 + 카카오뱅크 모임통장
- 문제점: 정원 기준 부재, 회원 상태 관리 미흡, 신규 가입 기준 불명확
- 총무(레로로) 2월 둘째 주부터 한 달간 부재 예정

---

## 🎯 핵심 요구사항

### 필수 기능 (MVP)
1. ✅ 신규 가입 신청 접수
2. ✅ 활동상태 변경 신청 접수 (활동중단/탈퇴/재개)
3. ✅ 정원 현황 표시 (정원/활동중/신규가능)
4. ✅ 관리자 승인/반려 기능
5. ✅ 회원 정보 조회 및 수정
6. ✅ 회칙 조회 (버전 관리)
7. ✅ 로그인 및 권한 관리 (일반회원/관리자)

### 제외 기능
- ❌ 회비 납입 관리 (카카오뱅크 모임통장 사용)
- ❌ 출석 체크
- ❌ 대시보드/통계
- ❌ 자동 알림/이메일 발송
- ❌ 복잡한 권한 체계

---

## 🏗️ 기술 스택

### Frontend
```
- React 18
- TypeScript
- Vite (빌드 도구)
- React Router v6 (라우팅)
- Tailwind CSS (스타일링)
- @supabase/supabase-js (DB 클라이언트)
```

### Backend
```
- Supabase
  - PostgreSQL (데이터베이스)
  - Auth (인증)
  - Row Level Security (권한 제어)
  - Realtime (선택적)
```

### 배포
```
- GitHub Pages 또는 Cloudflare Pages
- GitHub Actions (자동 배포 + Supabase keepalive)
```

### 개발 도구
```
- Claude Code (AI 개발 도구)
- Git & GitHub
- VS Code (권장)
```

---

## 💾 데이터베이스 스키마

### 1. members (회원)
```sql
CREATE TABLE members (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL, -- 해시 저장
  name TEXT NOT NULL,
  phone TEXT,
  status TEXT DEFAULT '활동중', -- 활동중/활동중단/대기/탈퇴
  role TEXT DEFAULT 'USER', -- USER/ADMIN
  joined_at DATE DEFAULT CURRENT_DATE,
  notes TEXT, -- 관리자 메모
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_members_email ON members(email);
CREATE INDEX idx_members_status ON members(status);
```

### 2. applications (가입 신청)
```sql
CREATE TABLE applications (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  created_at TIMESTAMP DEFAULT NOW(),
  name TEXT NOT NULL,
  email TEXT NOT NULL,
  phone TEXT NOT NULL,
  intro TEXT, -- 자기소개
  agreed_rules BOOLEAN DEFAULT FALSE, -- 회칙 동의
  agreed_privacy BOOLEAN DEFAULT FALSE, -- 개인정보 동의
  status TEXT DEFAULT '대기', -- 대기/승인/반려
  processed_at TIMESTAMP,
  processed_by TEXT, -- 처리자 이름
  reject_reason TEXT -- 반려 사유
);

-- 인덱스
CREATE INDEX idx_applications_status ON applications(status);
CREATE INDEX idx_applications_created_at ON applications(created_at DESC);
```

### 3. state_changes (활동상태 변경)
```sql
CREATE TABLE state_changes (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  created_at TIMESTAMP DEFAULT NOW(),
  member_id UUID REFERENCES members(id),
  member_name TEXT NOT NULL,
  from_status TEXT NOT NULL, -- 변경 전 상태
  to_status TEXT NOT NULL, -- 변경 요청 상태 (활동중단/탈퇴/활동재개)
  reason TEXT, -- 사유
  status TEXT DEFAULT '대기', -- 대기/승인/반려
  processed_at TIMESTAMP,
  processed_by TEXT
);

-- 인덱스
CREATE INDEX idx_state_changes_member_id ON state_changes(member_id);
CREATE INDEX idx_state_changes_status ON state_changes(status);
```

### 초기 데이터
```sql
-- 관리자 계정 생성 (비밀번호는 해시 처리 필요)
INSERT INTO members (email, password, name, role)
VALUES ('admin@swimteam.com', 'HASHED_PASSWORD', '관리자', 'ADMIN');
```

---

## 🎨 화면 구조 (총 11개)

### 비로그인 (3개)
1. **메인 페이지** (`/`)
   - 정원 현황 표시
   - 회칙 보기 링크
   - 가입 신청 버튼
   - 로그인 버튼

2. **회칙 페이지** (`/rules`)
   - 현재 회칙 전문 (v2.0)
   - 이전 버전 조회 (v1.0)
   - 정적 Markdown/HTML 파일

3. **가입 신청 페이지** (`/apply`)
   - 이름, 이메일, 연락처, 자기소개 입력
   - 회칙 동의 체크박스
   - 개인정보 동의 체크박스
   - 신청 버튼

### 로그인 (1개)
4. **로그인 페이지** (`/login`)
   - 이메일/비밀번호 입력
   - 로그인 상태 유지 옵션

### 일반회원 (3개)
5. **내 정보 페이지** (`/my`)
   - 이름, 이메일, 연락처, 상태, 가입일 표시
   - 연락처 수정
   - 비밀번호 변경
   - 활동상태 변경 신청 버튼

6. **회원 목록 페이지** (`/members`)
   - 전체 회원 이름 및 상태 표시
   - 상태별 필터링 (활동중/활동중단)

7. **활동상태 변경 페이지** (`/my/change-status`)
   - 현재 상태 표시
   - 변경 요청 (활동중단/탈퇴)
   - 사유 입력 (선택)

### 관리자 (4개)
8. **관리자 홈** (`/admin`)
   - 가입 신청 대기 건수
   - 상태변경 신청 대기 건수
   - 각 관리 페이지 링크

9. **가입신청 관리** (`/admin/applications`)
   - 신청 목록 (대기 중인 것 우선)
   - 신청자 정보 표시
   - 승인/반려 버튼

10. **회원 관리** (`/admin/members`)
    - 전체 회원 목록
    - 검색 기능
    - 회원별 수정 페이지 이동

11. **회원 수정** (`/admin/members/:id`)
    - 연락처 수정
    - 상태 변경 (활동중/활동중단/대기/탈퇴)
    - 비고 입력

---

## 🔐 권한 설계

### 역할 구분
| 역할 | 설명 |
|------|------|
| ADMIN | 관리자 (admin@swimteam.com) - 모든 권한 |
| USER | 일반 회원 - 제한된 권한 |

### 권한 매트릭스
| 기능 | ADMIN | USER |
|------|-------|------|
| 가입 신청 | ✅ | ✅ |
| 회칙 보기 | ✅ | ✅ |
| 내 정보 조회/수정 | ✅ | ✅ |
| 회원 목록 보기 | ✅ | ✅ |
| 활동상태 변경 신청 | ✅ | ✅ |
| 가입 신청 승인/반려 | ✅ | ❌ |
| 회원 정보 직접 수정 | ✅ | ❌ |
| 상태변경 승인/반려 | ✅ | ❌ |

### Supabase RLS 정책
```sql
-- members 테이블
-- 자기 정보는 조회 가능
CREATE POLICY "Users can view own data"
ON members FOR SELECT
USING (auth.uid() = id OR role = 'ADMIN');

-- 자기 정보는 수정 가능 (연락처, 비밀번호만)
CREATE POLICY "Users can update own contact"
ON members FOR UPDATE
USING (auth.uid() = id)
WITH CHECK (auth.uid() = id);

-- 관리자는 모든 회원 수정 가능
CREATE POLICY "Admins can update all members"
ON members FOR UPDATE
USING (
  EXISTS (
    SELECT 1 FROM members
    WHERE id = auth.uid() AND role = 'ADMIN'
  )
);

-- applications 테이블
-- 관리자만 조회 가능
CREATE POLICY "Only admins can view applications"
ON applications FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM members
    WHERE id = auth.uid() AND role = 'ADMIN'
  )
);

-- 누구나 신청 가능
CREATE POLICY "Anyone can apply"
ON applications FOR INSERT
WITH CHECK (true);

-- state_changes 테이블
-- 본인 신청 조회 가능, 관리자는 전체 조회
CREATE POLICY "Users can view own changes"
ON state_changes FOR SELECT
USING (
  member_id = auth.uid() OR
  EXISTS (
    SELECT 1 FROM members
    WHERE id = auth.uid() AND role = 'ADMIN'
  )
);
```

---

## 📂 프로젝트 구조

```
/swimteam-app
  ├── .github/
  │   └── workflows/
  │       ├── deploy.yml          # 자동 배포
  │       └── keepalive.yml       # Supabase 비활성 방지
  │
  ├── public/
  │   └── favicon.ico
  │
  ├── src/
  │   ├── components/
  │   │   ├── common/
  │   │   │   ├── Header.tsx
  │   │   │   ├── Button.tsx
  │   │   │   ├── StatusBadge.tsx
  │   │   │   └── ProtectedRoute.tsx
  │   │   │
  │   │   ├── forms/
  │   │   │   ├── ApplicationForm.tsx
  │   │   │   ├── ChangeStatusForm.tsx
  │   │   │   └── LoginForm.tsx
  │   │   │
  │   │   └── admin/
  │   │       ├── ApplicationCard.tsx
  │   │       ├── MemberEditForm.tsx
  │   │       └── MemberList.tsx
  │   │
  │   ├── pages/
  │   │   ├── HomePage.tsx
  │   │   ├── LoginPage.tsx
  │   │   ├── ApplyPage.tsx
  │   │   ├── RulesPage.tsx
  │   │   ├── MyPage.tsx
  │   │   ├── MembersPage.tsx
  │   │   ├── ChangeStatusPage.tsx
  │   │   │
  │   │   └── admin/
  │   │       ├── AdminHomePage.tsx
  │   │       ├── ApplicationsPage.tsx
  │   │       ├── MembersManagePage.tsx
  │   │       └── MemberEditPage.tsx
  │   │
  │   ├── hooks/
  │   │   ├── useAuth.ts
  │   │   ├── useMembers.ts
  │   │   ├── useApplications.ts
  │   │   └── useStateChanges.ts
  │   │
  │   ├── lib/
  │   │   ├── supabase.ts          # Supabase 클라이언트
  │   │   ├── types.ts             # TypeScript 타입 정의
  │   │   └── constants.ts         # 상수 정의
  │   │
  │   ├── contexts/
  │   │   └── AuthContext.tsx
  │   │
  │   ├── styles/
  │   │   └── globals.css
  │   │
  │   ├── App.tsx
  │   ├── routes.tsx
  │   └── main.tsx
  │
  ├── .env.local                   # 환경 변수 (gitignore)
  ├── .gitignore
  ├── package.json
  ├── vite.config.ts
  ├── tailwind.config.js
  ├── tsconfig.json
  └── README.md
```

---

## ⚙️ 환경 설정

### 환경 변수 (.env.local)
```bash
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### package.json 주요 의존성
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
```

---

## 🚀 개발 단계

### Phase 1: 프로젝트 초기화 (1일)
```bash
# 1. Vite로 React + TypeScript 프로젝트 생성
npm create vite@latest swimteam-app -- --template react-ts

# 2. 의존성 설치
cd swimteam-app
npm install
npm install @supabase/supabase-js react-router-dom
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 3. Supabase 프로젝트 생성
# https://supabase.com에서 새 프로젝트 생성
# URL과 API Key 복사

# 4. 환경 변수 설정
# .env.local 파일 생성 및 키 입력

# 5. Git 초기화
git init
git add .
git commit -m "Initial commit"
```

### Phase 2: DB 스키마 생성 (1일)
- Supabase SQL Editor에서 테이블 생성
- RLS 정책 설정
- 관리자 계정 생성

### Phase 3: 기본 구조 (2-3일)
- 라우팅 설정
- AuthContext 구현
- 공통 컴포넌트 (Header, Button 등)
- Supabase 연동 테스트

### Phase 4: 핵심 기능 개발 (1주)
- 로그인/로그아웃
- 가입 신청 폼
- 관리자 승인 기능
- 회원 목록
- 내 정보 수정
- 상태 변경 신청

### Phase 5: 회칙 페이지 (1일)
- Markdown 파일 작성
- 정적 페이지 렌더링

### Phase 6: 배포 (1일)
- GitHub Actions 설정
- GitHub Pages 또는 Cloudflare Pages 배포
- Keepalive 자동화 설정

---

## 📝 상세 요구사항

### 1. 가입 신청 프로세스
```
1. 사용자가 /apply 페이지 방문
2. 폼 입력 (이름, 이메일, 연락처, 자기소개)
3. 회칙 동의 체크 (필수)
4. 개인정보 동의 체크 (필수)
5. 신청 버튼 클릭
6. applications 테이블에 INSERT (status='대기')
7. 성공 메시지 표시
8. 관리자에게 알림 (수동 확인)
```

### 2. 관리자 승인 프로세스
```
1. 관리자가 /admin/applications 방문
2. 대기 중인 신청 목록 조회
3. 각 신청 검토
4. [승인] 클릭 시:
   - applications.status = '승인'
   - members 테이블에 새 회원 INSERT
   - 임시 비밀번호 생성 (예: 생년월일 또는 랜덤)
   - 이메일 발송 (수동 또는 자동)
5. [반려] 클릭 시:
   - applications.status = '반려'
   - reject_reason 입력
```

### 3. 활동상태 변경 프로세스
```
1. 사용자가 /my/change-status 방문
2. 변경 요청 선택 (활동중단/탈퇴)
3. 사유 입력 (선택)
4. 신청 버튼 클릭
5. state_changes 테이블에 INSERT (status='대기')
6. 관리자가 /admin에서 확인
7. 승인 시 members.status 업데이트
```

### 4. 정원 현황 계산
```typescript
// 정원: 16명 (고정)
const MAX_CAPACITY = 16;

// 활동중 인원 계산
const activeCount = await supabase
  .from('members')
  .select('*', { count: 'exact' })
  .eq('status', '활동중');

// 신규 가능 인원
const availableSlots = MAX_CAPACITY - activeCount;
```

---

## 🔒 보안 고려사항

### 1. 비밀번호
- bcrypt 또는 Supabase Auth 사용
- 최소 8자 이상
- 평문 저장 금지

### 2. 인증
- JWT 토큰 기반
- HttpOnly 쿠키 또는 localStorage
- 7일 자동 로그인

### 3. 권한
- Supabase RLS로 데이터 접근 제어
- 프론트엔드 라우팅 보호 (ProtectedRoute)
- API 요청 시 권한 재확인

### 4. XSS 방지
- React의 기본 XSS 방지 활용
- 사용자 입력 sanitization

---

## 📊 모니터링 및 유지보수

### GitHub Actions - Supabase Keepalive
```yaml
# .github/workflows/keepalive.yml
name: Supabase Keepalive

on:
  schedule:
    - cron: "0 1 * * 1,4"  # 매주 월/목 10시 (한국시간)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -fsS \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            "${{ secrets.SUPABASE_URL }}/rest/v1/members?select=count"
```

### 로그 관리
- Supabase Dashboard에서 쿼리 로그 확인
- 에러는 브라우저 콘솔 활용
- 필요시 Sentry 등 에러 추적 도구 추가

---

## 🎨 UI/UX 가이드

### 디자인 원칙
- **모바일 우선**: 모든 화면 640px 이하 최적화
- **접근성**: WCAG 2.1 AA 준수
- **단순함**: 최소한의 기능만 명확하게
- **일관성**: Tailwind 기본 색상 팔레트 활용

### 색상 체계
```
활동중: green-500
활동중단: yellow-500
대기: gray-500
탈퇴: red-500
```

### 반응형 브레이크포인트
```
모바일: 기본 (~ 640px)
태블릿: md (768px ~)
데스크톱: lg (1024px ~)
```

---

## 📌 참고사항

### 회칙 파일 관리
```
/public/rules/
  ├── v1.0.md    # 초기 회칙
  └── v2.0.md    # 현재 회칙

회칙 추가 시:
1. v3.0.md 파일 생성
2. RulesPage.tsx에서 버전 목록 업데이트
3. Git commit & push
```

### 개인정보 처리방침
```
/public/privacy/
  └── policy.html

내용:
- 수집 항목: 이름, 이메일, 연락처
- 수집 목적: 모임 운영, 회원 관리
- 보유 기간: 탈퇴 시까지
- 제3자 제공: 없음
```

---

## ✅ 개발 체크리스트

### 초기 설정
- [ ] Vite + React + TypeScript 프로젝트 생성
- [ ] Tailwind CSS 설정
- [ ] Supabase 프로젝트 생성
- [ ] 환경 변수 설정
- [ ] Git 저장소 초기화

### 데이터베이스
- [ ] members 테이블 생성
- [ ] applications 테이블 생성
- [ ] state_changes 테이블 생성
- [ ] RLS 정책 설정
- [ ] 관리자 계정 생성

### 기본 구조
- [ ] 라우팅 설정 (React Router)
- [ ] AuthContext 구현
- [ ] Supabase 클라이언트 설정
- [ ] 공통 컴포넌트 (Header, Button 등)

### 화면 개발
- [ ] 메인 페이지
- [ ] 로그인 페이지
- [ ] 가입 신청 페이지
- [ ] 회칙 페이지
- [ ] 내 정보 페이지
- [ ] 회원 목록 페이지
- [ ] 상태 변경 페이지
- [ ] 관리자 홈
- [ ] 가입 신청 관리
- [ ] 회원 관리
- [ ] 회원 수정

### 배포
- [ ] GitHub Actions 설정
- [ ] 자동 배포 설정
- [ ] Keepalive 설정
- [ ] 도메인 연결 (선택)

---

## 🔗 유용한 링크

- Supabase 문서: https://supabase.com/docs
- React Router: https://reactrouter.com
- Tailwind CSS: https://tailwindcss.com
- Vite: https://vitejs.dev

---

## 📞 문의 및 지원

- 개발자: 레로로 (총무)
- GitHub: [저장소 URL]
- 이관 시: 이 문서와 함께 .env.local 파일 전달

---

**마지막 업데이트**: 2025-01-20
**문서 버전**: 1.0