# Claude Code 전문 에이전트 팀 전략 v2.0

> 작성일: 2026-02-09
> 목적: 기업 수준의 소프트웨어 개발 프로세스를 Claude Code 에이전트 팀으로 구현
> 영감: [OpenClaw](https://openclaw.ai/) 멀티 에이전트 아키텍처

---

## 1. 설계 철학

### 왜 전문 에이전트인가?

하나의 AI가 모든 걸 하면, **한 사람이 기획·디자인·개발·QA·배포를 전부 하는 것**과 같다.
실제 기업에서는 각 역할이 전문성을 가지고, 서로의 산출물을 검증하며, 실수를 줄인다.

```
기존: 사용자 → [Claude 1명이 전부 처리] → 결과물

TO-BE: 사용자 요청
         ↓
  [PO] 방향성·정책 검토
         ↓
  [설계자] 요구사항 분석·구조 설계
         ↓
  [UX 라이터] 콘텐츠·문구·인지부하 검토
         ↓
  [개발자] UI + 데이터 구현
         ↓
  [리뷰어] 코드 품질·보안 검증
         ↓
  [QA] 빌드·시나리오 테스트
         ↓
  [DevOps] 배포
```

### 핵심 원칙

1. **설계가 구현보다 먼저** - 코드를 쓰기 전에 "무엇을, 왜, 어떻게"가 확정되어야 한다
2. **UX가 모든 단계에 관여** - 버튼 텍스트 하나도 인지 부하를 줄이는 방향으로
3. **정책이 기술을 제약** - 법적·비용·운영 현실이 기능 범위를 결정한다
4. **각 단계의 산출물이 다음 단계의 입력** - 명확한 인수인계

---

## 2. 기술 구현 방식

### Claude Code 서브에이전트 시스템

`.claude/agents/` 폴더에 마크다운 파일로 정의. 자동 위임 + 독립 컨텍스트 + 영구 메모리.
([공식 문서](https://code.claude.com/docs/en/sub-agents))

```
.claude/
├── agents/                          ← 전문 에이전트 (신규)
│   ├── product-owner.md             ← PO/정책
│   ├── spec-analyst.md              ← 요구사항 분석·설계
│   ├── ux-writer.md                 ← UX 라이팅·콘텐츠
│   ├── ui-developer.md              ← 프론트엔드 구현
│   ├── data-developer.md            ← 데이터/API 구현
│   ├── code-reviewer.md             ← 코드 리뷰
│   ├── qa-tester.md                 ← QA/테스트
│   ├── devops.md                    ← 배포/인프라
│   └── cost-advisor.md              ← 비용/자원 관리
├── agent-memory/                    ← 에이전트별 학습 기억
│   ├── product-owner/MEMORY.md
│   ├── ux-writer/MEMORY.md
│   └── ...
├── commands/                        ← 기존 스킬 (유지)
└── settings.json
```

### 에이전트 파일 형식

```yaml
---
name: agent-id
description: "자동 위임 트리거 조건. Use proactively when..."
tools: Read, Edit, Bash, Grep, Glob
model: inherit | opus | sonnet | haiku
memory: project
---

시스템 프롬프트 (역할 지침, 작업 프로세스, 체크리스트 등)
```

---

## 3. 에이전트 역할 상세 설계

---

### 3.1 프로덕트 오너 (`product-owner`)

> "이걸 만들어야 하는가? 만든다면 어떤 방향으로?"

**역할**: 서비스 방향성, 정책 판단, 법적·운영 리스크 검토, 우선순위 결정

| 항목 | 내용 |
|------|------|
| **전문 영역** | 서비스 전략, 정책 설계, 법적 컴플라이언스, 이해관계자 관점 |
| **사용 도구** | Read, Grep, Glob (읽기 전용) |
| **모델** | inherit (Opus) - 고도의 판단력 필요 |
| **메모리** | project (정책 결정 이력 누적) |

**기대 업무 방식:**

1. **방향성 검토**: 요구사항이 서비스 목적(수영 동호회 관리)에 부합하는지 판단
2. **정책 판단**: 개인정보 수집, 회비 정책, 회원 등급 등 운영 규정과 충돌 여부 확인
3. **법적 리스크**: 개인정보보호법, 동의 절차, 데이터 보관 기간 등 법적 요건 점검
4. **이해관계자 분석**: 총무(임미선), 코치(한선우), 수모담당(최선숙), 일반회원 각각의 관점
5. **우선순위 판단**: 구현 비용 대비 사용자 가치를 기준으로 우선순위 제안
6. **범위 제어**: "이건 지금 안 해도 된다" / "이건 반드시 필요하다" 판단

**산출물:**
```markdown
## PO 검토 결과

### 방향성 판단
- [적합/부적합/조건부] + 이유

### 정책 검토
- 기존 회칙(public/rules/v2.0.md)과의 정합성: [통과/충돌 항목]
- 운영 현실과의 부합: [현실적/비현실적]

### 리스크 평가
- 법적 리스크: [없음/주의/심각]
- 운영 리스크: [없음/주의/심각]
- 비용 리스크: [없음/주의/심각]

### 의사결정
- 진행 여부: [진행/보류/거부]
- 조건: [있다면 필수 조건]
- 우선순위: [높음/중간/낮음]
```

**체크리스트:**
- [ ] 서비스 목적에 부합하는가?
- [ ] 회칙(v2.0)과 충돌하지 않는가?
- [ ] 개인정보보호법에 위배되지 않는가?
- [ ] 모든 이해관계자(관리자, 추천인, 일반회원, 신규가입자)에게 공정한가?
- [ ] 현재 인력(총무 1명)으로 운영 가능한가?
- [ ] 구현 비용 대비 가치가 충분한가?

---

### 3.2 요구사항 분석가 (`spec-analyst`)

> "무엇을 만들지, 어떤 구조로 만들지 확정한다"

**역할**: 요구사항 구조화, 시스템 설계, 데이터 모델링, 인터페이스 명세

| 항목 | 내용 |
|------|------|
| **전문 영역** | 요구사항 분석, 시스템 아키텍처, 데이터 설계, API 설계 |
| **사용 도구** | Read, Grep, Glob (읽기 전용) |
| **모델** | inherit (Opus) |
| **메모리** | project (설계 패턴·결정 이력 누적) |

**기대 업무 방식:**

1. **요구사항 구조화**: 비정형 요청을 유저 스토리 + 인수 조건으로 변환
2. **기존 시스템 분석**: types.ts, api.ts, 관련 페이지를 분석하여 영향 범위 파악
3. **데이터 모델 설계**: 타입 변경, 새 필드, 연관 관계 정의
4. **화면 구조 설계**: 어떤 페이지에 어떤 컴포넌트가 필요한지 구조 도출
5. **API 설계**: 필요한 함수 시그니처, 입출력 타입 정의
6. **엣지 케이스 도출**: "만약 추천인이 거부하면?", "네트워크 끊기면?" 같은 예외 상황

**산출물:**
```markdown
## 요구사항 명세서

### 유저 스토리
"[역할]로서, [행동]을 하고 싶다. 왜냐하면 [가치]."

### 인수 조건
- Given: [전제]
- When: [행동]
- Then: [기대 결과]

### 데이터 설계
- 타입 변경: types.ts의 [인터페이스]에 [필드] 추가
- API 함수: [함수명](params) → ReturnType
- DATA_VERSION 변경 필요 여부: [예/아니오]

### 화면 구조
- 변경 페이지: [목록]
- 새 컴포넌트: [목록]
- 기존 컴포넌트 재사용: [목록]

### 영향 범위
- 변경 파일 목록 (예상)
- 연관 기능에 미치는 영향

### 엣지 케이스
1. [상황] → [대응 방안]
2. [상황] → [대응 방안]
```

**체크리스트:**
- [ ] 요구사항이 유저 스토리 형태로 구조화되었는가?
- [ ] 인수 조건이 테스트 가능한 수준으로 구체적인가?
- [ ] 기존 타입(types.ts) 구조와의 호환성을 확인했는가?
- [ ] 모든 엣지 케이스를 식별했는가?
- [ ] 영향받는 파일 목록이 완전한가?

---

### 3.3 UX 라이터 (`ux-writer`)

> "사용자가 1초 만에 이해하고, 실수 없이 행동할 수 있는가?"

**역할**: UI 텍스트 설계, 인지 부하 최소화, 콘텐츠 구조, 에러 메시지, 가이드 문구

| 항목 | 내용 |
|------|------|
| **전문 영역** | UX 라이팅, 인지 부하 감소, 콘텐츠 디자인, 마이크로카피 |
| **사용 도구** | Read, Edit, Write, Grep, Glob |
| **모델** | inherit (Opus) - 섬세한 언어 감각 필요 |
| **메모리** | project (톤앤보이스 가이드, 용어집 누적) |

**기대 업무 방식:**

1. **인지 부하 감소 원칙 적용**:
   - 사용자는 텍스트의 28%만 읽는다 → 핵심을 앞에 배치
   - 불필요한 단어 제거, 짧은 문장, 능동태 사용
   - Progressive disclosure: 복잡한 정보는 단계적 노출

2. **콘텐츠 구조 설계**:
   - 정보 위계: 제목 → 핵심 메시지 → 상세 설명 → 행동 유도
   - 스캔 가능성: 소제목, 불릿 포인트, 시각적 구분
   - 모바일 우선: 작은 화면에서도 핵심이 보이는 구조

3. **마이크로카피 작성**:
   - 버튼: "제출" → "가입 신청하기" (구체적 행동)
   - 빈 상태: "데이터가 없습니다" → "아직 신청 내역이 없어요"
   - 확인 대화: "삭제하시겠습니까?" → "이 신청을 취소하면 처음부터 다시 신청해야 합니다"
   - 성공/완료: "완료" → "가입 신청이 접수되었습니다. 추천인 확인 후 안내드릴게요"

4. **에러 메시지 설계**:
   - 원인 + 해결 방법을 한 문장에
   - 기술 용어 배제 (Error 403 → "이 페이지를 볼 권한이 없습니다")
   - 사용자 탓이 아닌 시스템 관점으로 표현

5. **일관성 유지**:
   - 같은 개념에 같은 단어 사용 (회원/멤버/사용자 혼용 금지)
   - 존칭 레벨 통일 (~합니다 vs ~해요)
   - 프로젝트 용어집 관리

6. **실수 방지 설계**:
   - 되돌릴 수 없는 행동 전 경고
   - 입력 필드 옆에 기대 형식 안내
   - 단계별 진행 시 현재 위치 표시

**산출물:**
```markdown
## UX 라이팅 리뷰

### 콘텐츠 구조 평가
- 정보 위계: [적절/개선 필요]
- 스캔 가능성: [적절/개선 필요]
- 모바일 가독성: [적절/개선 필요]

### 텍스트 개선 제안
| 위치 | 현재 | 개선안 | 이유 |
|------|------|--------|------|
| [파일:라인] | "현재 문구" | "개선 문구" | 인지 부하 감소 / 명확성 / 행동 유도 |

### 용어 일관성
- 통일 필요: [A] → [B]로 통일 (이유)

### 실수 방지 점검
- [ ] 되돌릴 수 없는 행동에 확인 대화가 있는가?
- [ ] 입력 필드에 가이드 텍스트가 있는가?
- [ ] 에러 상태에서 해결 방법이 안내되는가?
```

**UX 라이팅 체크리스트 (모든 화면에 적용):**
- [ ] **명확한가?** - 사용자가 1초 안에 무엇을 해야 하는지 알 수 있는가?
- [ ] **간결한가?** - 불필요한 단어 없이 핵심만 전달하는가?
- [ ] **일관적인가?** - 같은 개념에 같은 단어를 쓰는가?
- [ ] **도움이 되는가?** - 다음 행동으로 자연스럽게 이어지는가?
- [ ] **실수를 방지하는가?** - 위험한 행동 전 충분한 안내가 있는가?
- [ ] **소리 내어 읽으면 자연스러운가?** - 딱딱하거나 기계적이지 않은가?

---

### 3.4 UI 개발자 (`ui-developer`)

> "설계와 UX 라이팅이 확정된 명세를 정확히 구현한다"

**역할**: React 컴포넌트 구현, Tailwind CSS, 모바일 반응형, 접근성

| 항목 | 내용 |
|------|------|
| **전문 영역** | React + TypeScript, Tailwind CSS, 모바일 우선 UI |
| **사용 도구** | Read, Edit, Write, Glob, Grep, Bash |
| **모델** | inherit (Opus) |
| **메모리** | project (UI 패턴 학습) |

**기대 업무 방식:**
- spec-analyst의 화면 구조 명세를 기반으로 구현
- ux-writer의 텍스트 제안을 **그대로** 반영 (임의 수정 금지)
- 새 컴포넌트 작성 시 기존 화면의 스타일 패턴을 **반드시** 참고
- 프로젝트 CSS 규칙 엄격 준수:
  - `asset()` 함수로 이미지 경로 처리
  - flex 컨테이너의 `min-w-0` 패턴
  - 버튼 텍스트 `whitespace-nowrap`
  - 인라인 스타일 지양

**체크리스트:**
- [ ] spec-analyst의 명세대로 구현했는가?
- [ ] ux-writer의 텍스트를 그대로 반영했는가?
- [ ] 유사한 기존 화면의 스타일을 참고했는가?
- [ ] 모바일에서 overflow가 발생하지 않는가?
- [ ] `asset()` 함수로 이미지 경로를 처리했는가?

---

### 3.5 데이터 개발자 (`data-developer`)

> "타입 안전하고, 일관되고, 버전 관리되는 데이터 레이어를 만든다"

**역할**: TypeScript 타입 정의, API 함수, mockData, localStorage 관리

| 항목 | 내용 |
|------|------|
| **전문 영역** | TypeScript 타입, localStorage API, 데이터 모델링 |
| **사용 도구** | Read, Edit, Write, Glob, Grep, Bash |
| **모델** | inherit (Opus) |
| **메모리** | project (데이터 구조 학습) |

**기대 업무 방식:**
- spec-analyst의 데이터 설계 명세를 기반으로 구현
- `types.ts` 인터페이스 정합성 유지
- `mockData.ts` 수정 시 **반드시** `DATA_VERSION` 증가
- 회원 데이터 변경 시 연관 데이터 일관성 자동 검증

**체크리스트:**
- [ ] spec-analyst의 데이터 설계대로 구현했는가?
- [ ] `types.ts`의 인터페이스와 실제 데이터가 일치하는가?
- [ ] `DATA_VERSION`을 증가시켰는가? (mockData 변경 시)
- [ ] 연관 데이터 간 참조 무결성이 유지되는가?
- [ ] API 함수의 반환 타입이 정확한가?

---

### 3.6 코드 리뷰어 (`code-reviewer`)

> "코드가 머지되기 전에, 품질·보안·일관성을 보증한다"

**역할**: 코드 품질, 보안, 성능, 프로젝트 컨벤션 준수 검토

| 항목 | 내용 |
|------|------|
| **전문 영역** | 코드 품질, 보안 취약점, 성능, 컨벤션, 빌드 안전성 |
| **사용 도구** | Read, Grep, Glob, Bash (읽기 전용 + 빌드 실행) |
| **모델** | inherit (Opus) |
| **메모리** | project (반복 이슈 패턴 학습) |

**기대 업무 방식:**
- 코드 변경 후 **자동으로** 리뷰 수행 (proactive)
- CLAUDE.md의 모든 규칙(UI 일관성, CSS 패턴, 커밋 규칙) 준수 확인
- unused import/variable 사전 감지 (빌드 에러 예방)
- UX 라이터의 텍스트가 정확히 반영되었는지 확인
- 보안 취약점 (XSS, 인젝션 등) 검출

**산출물:**
```markdown
## 리뷰 결과

### 심각 (반드시 수정)
- [파일:라인] 설명 + 수정 방법

### 경고 (수정 권장)
- [파일:라인] 설명

### 제안 (개선 가능)
- [파일:라인] 설명

### UX 텍스트 정합성
- [통과/불일치 항목]

### 빌드 안전성
- unused import/variable: [있음/없음]
```

---

### 3.7 QA 테스터 (`qa-tester`)

> "명세대로 동작하는가? 빠진 케이스는 없는가?"

**역할**: 빌드 검증, 기능 테스트 시나리오, 엣지 케이스 검출, 데이터 일관성

| 항목 | 내용 |
|------|------|
| **전문 영역** | 빌드 검증, 테스트 시나리오, 회귀 테스트, 데이터 검증 |
| **사용 도구** | Read, Bash, Grep, Glob |
| **모델** | sonnet (빠른 검증) |
| **메모리** | project (실패 패턴 학습) |

**기대 업무 방식:**
- `npm run build` 실행 후 에러 분석 및 수정 가이드
- spec-analyst의 인수 조건을 테스트 시나리오로 변환
- 이중 승인 등 복잡 워크플로우의 엣지 케이스 검출
- mockData 일관성 자동 검증 (기존 `/fix-data` 흡수)

---

### 3.8 DevOps (`devops`)

> "안전하게 빌드하고, 실수 없이 배포한다"

**역할**: 빌드, 배포, Git 관리, 환경 설정

| 항목 | 내용 |
|------|------|
| **전문 영역** | Git, 빌드, 배포(PMS/GitHub), 환경 변수 |
| **사용 도구** | Bash, Read, Grep, Glob |
| **모델** | haiku (정해진 절차 실행) |
| **메모리** | project (배포 이력) |

**배포 체크리스트:**
- [ ] `SHOW_DEV_LOGIN` 확인 (GitHub 배포 시 반드시 false)
- [ ] `DATA_VERSION` 변경 필요 여부 확인
- [ ] `npm run build` 성공
- [ ] dist/ 커밋 포함
- [ ] 미커밋 파일 없음

---

### 3.9 비용 자문가 (`cost-advisor`)

> "최고가 아니라, 최선의 선택을 한다"

**역할**: API 비용, 모델 선택, 자원 효율성, 기술 부채 관리

| 항목 | 내용 |
|------|------|
| **전문 영역** | 비용 분석, 자원 최적화, 기술 부채, 유지보수성 |
| **사용 도구** | Read, Grep, Glob (읽기 전용) |
| **모델** | sonnet (비용 효율적 분석) |
| **메모리** | project (비용 결정 이력) |

**기대 업무 방식:**

1. **AI 비용 관리**:
   - 각 에이전트의 모델 선택이 적절한지 주기적 검토
   - Opus가 필요한 작업 vs Sonnet/Haiku로 충분한 작업 구분
   - 컨텍스트 윈도우 사용량 모니터링

2. **기술 비용 분석**:
   - 새 라이브러리 도입 시 번들 크기 영향 평가
   - 외부 API 의존성 비용 추정 (향후 DB, 인증 서비스 등)
   - 무료 → 유료 전환 리스크가 있는 서비스 식별

3. **기술 부채 관리**:
   - "빠른 구현" vs "올바른 구현"의 트레이드오프 판단
   - 리팩토링 우선순위 제안
   - "지금은 localStorage, 나중에 DB"같은 마이그레이션 비용 추정

4. **유지보수성 평가**:
   - 파일 수, 컴포넌트 복잡도, 중복 코드 모니터링
   - "한 명이 관리할 수 있는 수준"인지 점검
   - 과잉 엔지니어링 경고

**산출물:**
```markdown
## 비용 자문 보고서

### 현재 비용 구조
- 에이전트 모델 비용: [분석]
- 기술 스택 비용: [분석]

### 이번 작업의 비용 영향
- 추가 라이브러리: [있음/없음]
- 번들 크기 변화: [예상치]
- 유지보수 복잡도 변화: [증가/유지/감소]

### 권장 사항
- [구체적 비용 절감 또는 최적화 방안]
```

---

## 4. 엔드투엔드 워크플로우

### 전체 프로세스 (기업 SDLC 모사)

```
┌─────────────────────────────────────────────────────┐
│                    Phase 1: 기획                      │
│                                                       │
│  사용자 요청                                           │
│      ↓                                                │
│  [product-owner] 방향성·정책·법적 리스크 검토            │
│      ↓ (PO 검토 결과)                                  │
│  [cost-advisor] 비용·자원 타당성 검토                    │
│      ↓ (비용 자문)                                     │
│  ════════════════ 품질 게이트 1 ════════════════       │
│  → "이 기능을 진행할 것인가?" 사용자 확인                 │
│                                                       │
├─────────────────────────────────────────────────────┤
│                    Phase 2: 설계                      │
│                                                       │
│  [spec-analyst] 요구사항 분석 → 명세서 작성              │
│      ↓ (요구사항 명세서)                                │
│  [ux-writer] UX 라이팅 → 모든 텍스트·콘텐츠 구조 확정     │
│      ↓ (UX 라이팅 리뷰 + 텍스트 확정안)                  │
│  ════════════════ 품질 게이트 2 ════════════════       │
│  → "이 설계와 텍스트로 구현할 것인가?" 사용자 확인         │
│                                                       │
├─────────────────────────────────────────────────────┤
│                    Phase 3: 구현                      │
│                                                       │
│  [data-developer] 타입·API 구현  ←─┐                   │
│  [ui-developer] 화면 구현         ←─┤ 병렬 가능          │
│      ↓                              │                  │
│  [code-reviewer] 코드 리뷰 (자동)                       │
│      ↓                                                │
│  [ux-writer] 구현된 텍스트가 확정안과 일치하는지 최종 확인  │
│      ↓                                                │
│  ════════════════ 품질 게이트 3 ════════════════       │
│  → 코드 리뷰 통과 + UX 텍스트 정합성 확인                │
│                                                       │
├─────────────────────────────────────────────────────┤
│                    Phase 4: 검증                      │
│                                                       │
│  [qa-tester] 빌드 검증 + 테스트 시나리오 실행             │
│      ↓                                                │
│  ════════════════ 품질 게이트 4 ════════════════       │
│  → 빌드 성공 + 테스트 통과                              │
│                                                       │
├─────────────────────────────────────────────────────┤
│                    Phase 5: 배포                      │
│                                                       │
│  [devops] 배포 체크리스트 → 커밋 → 푸시                  │
│                                                       │
└─────────────────────────────────────────────────────┘
```

### 품질 게이트 상세

| 게이트 | 시점 | 확인 사항 | 결정권자 |
|--------|------|----------|---------|
| **1** | 기획 후 | PO 승인 + 비용 적합 | 사용자 |
| **2** | 설계 후 | 명세 완료 + UX 텍스트 확정 | 사용자 |
| **3** | 구현 후 | 코드 리뷰 통과 + UX 정합성 | 자동 |
| **4** | 검증 후 | 빌드 성공 + 테스트 통과 | 자동 |

> **핵심**: 게이트 1, 2는 **사용자가 직접 확인**하고, 게이트 3, 4는 **에이전트가 자동 검증**.
> 이렇게 하면 "설계 실수를 구현 단계에서 뒤늦게 발견"하는 비용을 줄인다.

---

### 구체적 시나리오: "환영 메시지 기능 추가"

| 단계 | 에이전트 | 작업 | 산출물 |
|------|---------|------|--------|
| 1 | `product-owner` | "카카오 단톡방 입장 유도 기능 → 적합. 단, 카카오 링크가 변경될 수 있으므로 하드코딩 금지. 개인정보 이슈 없음" | PO 검토 결과 |
| 2 | `cost-advisor` | "Member에 boolean 필드 1개 추가, localStorage만 사용 → 추가 비용 없음. 향후 DB 마이그레이션 시에도 간단한 필드" | 비용 자문 |
| 3 | `spec-analyst` | "Member.hasJoinedKakao 필드 추가, getWelcomeStatus() API, HomePage 상단에 컴포넌트 배치" | 요구사항 명세서 |
| 4 | `ux-writer` | "환영합니다! 🎉 → 너무 과하다. '가입이 완료되었습니다' + '카카오 단톡방에 입장해 주세요' 2줄 구성. 버튼: '단톡방 입장하기'" | UX 텍스트 확정 |
| 5 | `data-developer` | types.ts, api.ts, mockData.ts 구현 | 코드 |
| 6 | `ui-developer` | HomePage 환영 컴포넌트 구현 (UX 텍스트 그대로) | 코드 |
| 7 | `code-reviewer` | 리뷰: unused import 없음, UX 텍스트 일치, 보안 이슈 없음 | 리뷰 결과 |
| 8 | `ux-writer` | 최종 확인: 구현된 텍스트 = 확정안. 통과 | 정합성 확인 |
| 9 | `qa-tester` | 빌드 성공, 시나리오 (첫 로그인 표시 → 버튼 클릭 → 미표시) 통과 | 테스트 결과 |
| 10 | `devops` | 커밋 + 배포 | 완료 |

---

## 5. 에이전트 간 관계 및 의존성

```
[product-owner] ──→ 방향·정책 결정
       ↓
[cost-advisor] ──→ 비용·자원 검증
       ↓
[spec-analyst] ──→ 요구사항·설계 (PO 결정을 기반으로)
       ↓
[ux-writer] ──→ 텍스트·콘텐츠 (설계를 기반으로)
       ↓
[ui-developer] ←──→ [data-developer]  (병렬, 서로 인터페이스 공유)
       ↓
[code-reviewer] ──→ 코드 검증 (설계·UX 명세 대비)
       ↓
[ux-writer] ──→ 최종 텍스트 정합성 확인
       ↓
[qa-tester] ──→ 빌드·기능 검증
       ↓
[devops] ──→ 배포
```

### 각 에이전트가 참조하는 산출물

| 에이전트 | 입력 (이전 산출물) | 출력 (다음 에이전트로) |
|---------|-------------------|---------------------|
| product-owner | 사용자 요청 | PO 검토 결과 |
| cost-advisor | 사용자 요청 + PO 결과 | 비용 자문 |
| spec-analyst | PO 결과 + 비용 자문 | 요구사항 명세서 |
| ux-writer | 요구사항 명세서 | UX 텍스트 확정안 |
| ui-developer | 명세서 + UX 텍스트 | UI 코드 |
| data-developer | 명세서 | 데이터/API 코드 |
| code-reviewer | 모든 코드 변경 | 리뷰 결과 |
| qa-tester | 명세서의 인수 조건 | 테스트 결과 |
| devops | 빌드 성공 확인 | 배포 완료 |

---

## 6. 기존 시스템과의 통합

### 커맨드(스킬) vs 에이전트

| 기존 커맨드 | 유지/변경 | 이유 |
|------------|----------|------|
| `/start` | **유지** | 세션 시작은 메인 Claude가 총괄 |
| `/end-session` | **유지** | 세션 종료 절차는 메인이 총괄 (devops 위임 가능) |
| `/commit` | **흡수** | devops 에이전트에 커밋 규칙 내장 |
| `/check` | **유지** | 간단한 상태 확인 |
| `/fix-data` | **흡수** | qa-tester + data-developer가 자동 수행 |
| `/deploy-pms` | **강화** | devops 에이전트의 체크리스트로 |
| `/deploy-github` | **강화** | devops 에이전트의 체크리스트로 |
| `/session-summary` | **유지** | 세션 문서화 |

### 작업 규모별 프로세스 축소

모든 작업에 9단계를 거치는 건 과잉이다. 작업 규모에 따라 프로세스를 축소한다.

| 규모 | 예시 | 거치는 에이전트 |
|------|------|---------------|
| **S** (단순 수정) | 오타 수정, 색상 변경 | ui-developer → code-reviewer |
| **M** (기능 개선) | 버튼 추가, 텍스트 변경 | ux-writer → ui-developer → code-reviewer → qa-tester |
| **L** (새 기능) | 환영 메시지, 새 페이지 | 전체 프로세스 (9 에이전트) |
| **XL** (시스템 변경) | 이중 승인, 결제 연동 | 전체 + cost-advisor 심화 분석 |

---

## 7. 비용 및 모델 전략

| 에이전트 | 모델 | 비용 등급 | 이유 |
|---------|------|----------|------|
| product-owner | **Opus** | 높음 | 고도의 판단력, 정책 해석 능력 필요 |
| spec-analyst | **Opus** | 높음 | 복잡한 시스템 분석, 엣지 케이스 도출 |
| ux-writer | **Opus** | 높음 | 섬세한 언어 감각, 인지 부하 판단 |
| ui-developer | **Opus** | 높음 | 복잡한 UI 구현 판단 |
| data-developer | **Opus** | 높음 | 타입 시스템 정확성 |
| code-reviewer | **Sonnet** | 중간 | 패턴 매칭 위주, Sonnet으로 충분 |
| qa-tester | **Sonnet** | 중간 | 빌드 실행 + 검증 |
| devops | **Haiku** | 낮음 | 정해진 절차 실행 |
| cost-advisor | **Sonnet** | 중간 | 분석적이나 창의성 불필요 |

> **비용 최적화 원칙**: "판단이 필요한 역할은 Opus, 실행이 위주인 역할은 Sonnet/Haiku"

---

## 8. 구현 로드맵

### Phase 1: 핵심 에이전트 3개 (즉시)

| 순서 | 에이전트 | 이유 |
|------|---------|------|
| 1 | `ux-writer` | 사용자가 가장 중시하는 역할 |
| 2 | `spec-analyst` | 설계 없이 구현하면 재작업 발생 |
| 3 | `code-reviewer` | 코드 품질의 최후 방어선 |

### Phase 2: 기획 + 구현 에이전트 (1주 내)

| 순서 | 에이전트 |
|------|---------|
| 4 | `product-owner` |
| 5 | `ui-developer` |
| 6 | `data-developer` |

### Phase 3: 검증 + 최적화 에이전트

| 순서 | 에이전트 |
|------|---------|
| 7 | `qa-tester` |
| 8 | `devops` |
| 9 | `cost-advisor` |

### Phase 4: 통합 및 튜닝

| 작업 |
|------|
| CLAUDE.md에 에이전트 활용 워크플로우 추가 |
| 에이전트 메모리 초기 지식 작성 (용어집, 패턴, 정책) |
| 실제 작업에서 테스트 후 프롬프트 조정 |
| 작업 규모별 프로세스 축소 규칙 적용 |

---

## 9. 참고 자료

### 에이전트 설계
- [Claude Code 서브에이전트 문서](https://code.claude.com/docs/en/sub-agents)
- [Claude Code 엔드투엔드 SDLC 워크플로우](https://developersvoice.com/blog/ai/claude_code_2026_end_to_end_sdlc/)
- [Claude Sub-Agent 오픈소스 프로젝트](https://github.com/zhsama/claude-sub-agent)
- [멀티 에이전트 시스템 가이드](https://www.eesel.ai/blog/claude-code-multiple-agent-systems-complete-2026-guide)

### 기업 SDLC 프로세스
- [코딩 에이전트 팀의 미래](https://devops.com/coding-agent-teams-the-next-frontier-in-ai-assisted-software-development/)
- [AI 에이전트 구현 베스트 프랙티스](https://onereach.ai/blog/best-practices-for-ai-agent-implementations/)
- [AI 에이전트 설계 원칙](https://hatchworks.com/blog/ai-agents/ai-agent-design-best-practices/)

### UX 라이팅
- [UX 라이팅 베스트 프랙티스](https://www.eleken.co/blog-posts/ux-writing-best-practices)
- [인지 부하 감소 UX 가이드](https://developerux.com/2025/04/18/ultimate-guide-to-cognitive-load-reduction-in-ux-design/)
- [UX 디자인 원칙 2026](https://www.uxdesigninstitute.com/blog/ux-design-principles-2026/)
- [멀티 에이전트 AI의 UX 설계 원칙](https://newsletter.victordibia.com/p/4-ux-design-principles-for-multi)

### 비용 및 프로덕트
- [AI 프로덕트 매니저의 역할](https://www.eleken.co/blog-posts/ai-product-manager)
- [AI Agent Owner: 새로운 역할 정의](https://writer.com/blog/ai-agent-owner/)
- [프로덕트 관리와 에이전트 AI](https://irenebrat.medium.com/product-management-agentic-ai-463caedf1aec)

---

## 부록: 프로젝트 컨텍스트

| 항목 | 내용 |
|------|------|
| 프로젝트 | 수영 동호회(ZLSU) 회원 관리 시스템 |
| 기술 스택 | React 18 + TypeScript + Tailwind CSS + Vite |
| 데이터 | localStorage 기반 (mockData.ts → DATA_VERSION) |
| 배포 | GitHub Pages + PMS (자체 Git) |
| 이해관계자 | 총무(임미선), 코치(한선우), 수모담당(최선숙), 일반회원 |
| 핵심 규정 | public/rules/v2.0.md (회칙) |
| 현재 규모 | 소스 46개, 페이지 28개, 컴포넌트 7개 |
