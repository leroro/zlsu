# 세션 작업 내역 (2026-01-24 #04)

## 작업 요약

GitHub Pages 배포 설정 및 이미지 경로 문제 해결

## 완료된 작업

### 1. GitHub Pages 배포 설정

- GitHub remote 추가: `git@github.com:leroro/zlsu.git`
- GitHub Actions 워크플로우 생성 (`.github/workflows/deploy.yml`)
- 배포 URL: https://leroro.github.io/zlsu/

### 2. 배포 스킬 분리

두 개의 저장소를 구분하여 배포 스킬 생성:

| 스킬 | 용도 | 저장소 |
|------|------|--------|
| `/deploy-pms` | 개발/테스트 | PMS (origin) |
| `/deploy-github` | 회원 공개 | GitHub Pages (github) |

### 3. 이미지 경로 문제 해결

**문제:** `./images/...` 경로가 로컬 개발 서버에서 동작하지 않음

**해결:** `asset()` 유틸리티 함수 생성

```tsx
// src/lib/assets.ts
export const asset = (path: string): string => {
  return `${import.meta.env.BASE_URL}${path}`;
};

// 사용법
import { asset } from '../lib/assets';
<img src={asset('images/logo.svg')} />
```

**수정된 파일:**
- `src/pages/HomePage.tsx`
- `src/pages/AboutPage.tsx`
- `src/components/common/Header.tsx`

### 4. CLAUDE.md 업데이트

- 배포 정책 추가 (PMS vs GitHub)
- 이미지 경로 규칙 추가 (`asset()` 함수 필수 사용)

## 기술 참고

- HashRouter 사용으로 `base: './'` (상대경로)가 모든 환경에서 동작
- vite.config.ts 수정 불필요
- Supabase 연결 시에도 현재 설정 그대로 사용 가능

## 변경된 파일

- `.github/workflows/deploy.yml` (신규)
- `.claude/commands/deploy-pms.md` (이름 변경)
- `.claude/commands/deploy-github.md` (신규)
- `src/lib/assets.ts` (신규)
- `src/pages/HomePage.tsx`
- `src/pages/AboutPage.tsx`
- `src/components/common/Header.tsx`
- `CLAUDE.md`
- `vite.config.ts`
