# 활동 포인트 시스템 설계

> **작성일:** 2026-01-23
> **상태:** 설계 단계 (미구현)
> **관련 파일:** 이 문서만으로 다음 세션에서 구현 가능하도록 작성

---

## 현재 시스템 상태

### 관련 파일 위치
- **타입 정의:** `src/lib/types.ts`
- **API 함수:** `src/lib/api.ts`
- **상수:** `src/lib/constants.ts`
- **회원 명단 페이지:** `src/pages/MembersPage.tsx`
- **설정 관리:** `src/pages/admin/SettingsPage.tsx`

### 현재 활동지수 구조

**타입 (`src/lib/types.ts`):**
```typescript
export type ActivityLevel = 'newbie' | 'regular' | 'passionate' | 'core' | 'staff';
```

**상수 (`src/lib/constants.ts`):**
```typescript
export const ACTIVITY_LEVELS: ActivityLevel[] = ['newbie', 'regular', 'passionate', 'core', 'staff'];
export const ACTIVITY_LEVEL_LABELS: Record<ActivityLevel, string> = {
  newbie: '뉴비', regular: '일반', passionate: '열정', core: '핵심', staff: '스태프'
};
export const ACTIVITY_LEVEL_ICONS: Record<ActivityLevel, string> = {
  newbie: '🛟', regular: '🏊', passionate: '🐬', core: '🦈', staff: '🏆'
};
```

**Member 인터페이스에 있는 관련 필드:**
```typescript
interface Member {
  // ... 기타 필드
  activityLevel?: ActivityLevel;  // 현재 수동 관리
  position?: string;              // 담당 역할 (수영 지도, 수모 담당 등)
}
```

### 현재 회원 명단 탭 구조
- **명단 탭:** 상세 정보 (자기소개, 주종목)
- **순위 탭:** 레벨별 정렬 (현재는 카테고리 기반, 실제 순위 아님)
- **생일 탭:** 월별 생일자

### 현재 문제점
1. "순위"라고 하지만 실제 점수 없이 레벨 카테고리로만 정렬
2. 활동지수 레벨이 관리자 수동 관리
3. 스태프가 활동지수 레벨에 포함되어 있으나, 실제로는 역할(Role)임
4. 출석 데이터가 카카오톡 캘린더에서 별도 관리 중

---

## 배경

현재 "순위" 탭에서 활동지수 레벨(스태프 > 핵심 > 열정 > 일반 > 뉴비)로 정렬하고 있으나, 이는 카테고리일 뿐 실제 "순위"라고 하기 어려움. 의미 있는 순위를 위해서는 포인트 기반 시스템이 필요.

---

## 핵심 고민 사항

### 1. 스태프 라벨 vs 활동 포인트 분리

**문제점:**
- 현재 "스태프"는 활동지수 레벨 중 하나로 취급됨
- 그러나 스태프는 역할(Role)이지, 활동량을 나타내는 것이 아님
- 스태프도 참석할 때가 있고 안 할 때가 있음
- 휴면 시에도 스태프 라벨은 유지되어야 함

**해결 방향:**
```
┌─────────────────────────────────────────────────┐
│  역할 (Role)         │  활동 포인트 (Points)      │
├─────────────────────────────────────────────────┤
│  - 스태프 (운영진)    │  - 누적 포인트로 계산      │
│  - 일반 회원         │  - 순위 산정에 사용        │
│                      │  - 레벨 자동 산정          │
└─────────────────────────────────────────────────┘
```

- **역할(position)**: 수영 지도, 수모 담당, 총무 담당 등 → 이미 존재
- **활동 포인트**: 새로 추가, 참여도 기반 점수
- **활동 레벨**: 포인트 구간에 따라 자동 산정 (뉴비 → 일반 → 열정 → 핵심)

---

### 2. 포인트 부여 기준

| 일정 유형 | 설명 | 포인트 (예시) |
|-----------|------|---------------|
| 정기 연습 | 매주 토요일 수영 | 1점 |
| 이벤트 | 송년회, 점심 모임 등 계획된 모임 | 5점 |
| 대회 | 수영 대회 참가 | 10점 |
| 티타임 | 자유 모임 | 미적용 (추적 안 함) |

**관리자 설정:**
- 각 일정 유형별 포인트 가중치를 관리자가 설정 가능
- 설정 화면에서 변경 가능

---

### 3. 포인트 누적 정책

**고려해야 할 옵션:**

| 옵션 | 설명 | 장점 | 단점 |
|------|------|------|------|
| 영구 누적 | 가입 후 계속 누적 | 장기 회원 우대, 기록 보존 | 신규 회원 진입장벽 |
| 연간 리셋 | 매년 1월 초기화 | 공정한 경쟁, 동기부여 | 기존 기록 손실 |
| 롤링 기간 | 최근 6개월/12개월만 집계 | 최근 활동 반영, 균형 | 구현 복잡 |

**추천:** 롤링 12개월 또는 연간 리셋 (결정 필요)

---

### 4. 활동 레벨 자동 산정

포인트 구간에 따른 레벨 자동 부여:

| 레벨 | 포인트 구간 (예시) | 조건 |
|------|-------------------|------|
| 뉴비 | 0 ~ 10점 | 가입 후 2개월 이내 또는 |
| 일반 | 11 ~ 30점 | 기본 참여 |
| 열정 | 31 ~ 60점 | 월 3회 이상 꾸준히 |
| 핵심 | 61점 이상 | 대회 참가 + 고참여 |

**참고:** 스태프 라벨은 별도 (역할 기반, 포인트 무관)

---

## 필요한 기능

### 1. 일정 관리

**일정 유형:**
- 정기 연습 (매주 토요일 자동 생성 또는 수동)
- 이벤트 (관리자가 수동 등록)
- 대회 (관리자가 수동 등록)

**일정 정보:**
```typescript
interface Schedule {
  id: string;
  type: 'regular' | 'event' | 'competition';
  title: string;
  date: string;           // YYYY-MM-DD
  deadline: string;       // 출석 체크 마감 (예: 금요일 자정)
  points: number;         // 참여 시 부여 포인트
  createdAt: string;
  createdBy: string;
}
```

### 2. 출석 체크

**회원 화면:**
- 홈 화면에 "이번 주 출석체크" 카드
- [참여] [불참] 버튼
- 마감 시간 표시

**데이터 구조:**
```typescript
interface Attendance {
  id: string;
  scheduleId: string;
  memberId: string;
  status: 'attending' | 'not_attending';
  checkedAt: string;
}
```

### 3. 포인트 관리

**포인트 로그:**
```typescript
interface PointLog {
  id: string;
  memberId: string;
  scheduleId: string;     // 연결된 일정
  points: number;         // 부여된 포인트
  reason: string;         // "정기 연습 참여", "송년회 참여" 등
  createdAt: string;
}
```

**회원별 포인트 조회:**
- 현재 총 포인트
- 포인트 히스토리
- 현재 레벨

### 4. 관리자 설정

**포인트 가중치 설정:**
```typescript
interface PointSettings {
  regularPractice: number;  // 정기 연습 (기본 1점)
  event: number;            // 이벤트 (기본 5점)
  competition: number;      // 대회 (기본 10점)
}
```

**레벨 구간 설정:**
```typescript
interface LevelThresholds {
  regular: number;    // 일반 최소 포인트
  passionate: number; // 열정 최소 포인트
  core: number;       // 핵심 최소 포인트
}
```

---

## 화면 설계

### 1. 회원 홈 화면 - 출석체크 UI

#### 현재 "자주 찾는 메뉴" 섹션 구조
```
현재:
┌─────────────────────────────────────┐
│ 자주 찾는 메뉴                       │
│                                     │
│ [팀 카톡방 입장]  [수모 추가 구입 안내] │
└─────────────────────────────────────┘
```

#### 변경 후 구조
```
┌─────────────────────────────────────┐
│ 자주 찾는 메뉴                       │
│                                     │
│ [팀 카톡방 입장]  [이번 주 출석체크]   │
└─────────────────────────────────────┘
```

**"이번 주 출석체크" 버튼 상태:**

1. **미신청 상태 (기본):**
   ```
   [📅 이번 주 출석체크]
   1/25(토) 정기 연습
   ```

2. **참석 신청 완료:**
   ```
   [✓ 참석 신청됨]      (녹색 배경)
   1/25(토) 정기 연습
   ```

3. **불참 신청 완료:**
   ```
   [✗ 불참 신청됨]      (빨간 배경)
   1/25(토) 정기 연습
   ```

4. **이번 주 일정 없음:**
   ```
   [📅 이번 주 일정 없음]  (비활성화)
   ```

#### 출석체크 버튼 클릭 시 동작

**Option A: 바텀시트/모달**
```
┌─────────────────────────────────────┐
│ 1월 25일 (토) 정기 연습              │
│ 마감: 1월 24일 (금) 자정까지          │
├─────────────────────────────────────┤
│                                     │
│  [참석]    [불참]    [미정]          │
│                                     │
│  현재 참석 8명 / 불참 3명 / 미정 3명   │
└─────────────────────────────────────┘
```

**Option B: 인라인 토글 (권장)**
버튼 클릭 시 바로 3개 옵션이 펼쳐짐:
```
┌─────────────────────────────────────┐
│ 📅 1/25(토) 정기 연습                │
│                                     │
│ ○ 참석   ● 불참   ○ 미정            │
│                                     │
│ 마감: 금요일 자정 · 참석 8 / 불참 3   │
└─────────────────────────────────────┘
```

---

### 2. 일정 보기 페이지 (신규)

**경로:** `/schedules` 또는 `/calendar`

**목적:**
- 이번 주 뿐 아니라 과거/미래 일정 조회
- 내 출석 기록 확인
- 다가오는 일정 미리 확인

**UI 구조:**
```
┌─────────────────────────────────────┐
│ 📅 일정                             │
├─────────────────────────────────────┤
│ [이번 주]  [다음 주]  [지난 일정]     │
├─────────────────────────────────────┤
│                                     │
│ 1월 25일 (토) 정기 연습              │
│ ✓ 참석 신청됨                       │
│ 참석 8 / 불참 3 / 미정 3            │
│                                     │
│ ─────────────────────────────────── │
│                                     │
│ 2월 1일 (토) 정기 연습               │
│ 아직 신청 안 함  [신청하기]          │
│                                     │
│ ─────────────────────────────────── │
│                                     │
│ 2월 8일 (토) 송년회                  │
│ 🏷️ 이벤트 · 5점                     │
│ 아직 신청 안 함  [신청하기]          │
│                                     │
└─────────────────────────────────────┘
```

**지난 일정 보기:**
```
┌─────────────────────────────────────┐
│ 1월 18일 (토) 정기 연습              │
│ ✓ 참석 · +1점 획득                  │
│                                     │
│ 1월 11일 (토) 정기 연습              │
│ ✗ 불참                              │
│                                     │
│ 1월 4일 (토) 정기 연습               │
│ ✓ 참석 · +1점 획득                  │
└─────────────────────────────────────┘
```

---

### 3. 회원 홈 - 기존 출석체크 카드 (대안)

**순위 탭 → 활동지수 탭:**
```
┌─────────────────────────────────────┐
│ 🏆 내 활동지수                       │
│                                     │
│ 현재 포인트: 45점                    │
│ 레벨: 열정 🐬                        │
│ 순위: 3위 / 14명                     │
│                                     │
│ "열정이 넘치네요! 🔥"                 │
└─────────────────────────────────────┘

[레벨별 회원 목록]
🏆 핵심 (2명)
  1. 한선우 (52점)
  2. 김철수 (48점)

🐬 열정 (3명)
  3. 나 이미선 (45점)
  4. 박영희 (42점)
  ...
```

### 2. 관리자 화면

**일정 관리:**
- 일정 목록 (달력 또는 리스트)
- 일정 등록/수정/삭제
- 출석 현황 조회

**설정 - 포인트:**
- 일정 유형별 포인트 가중치
- 레벨 구간 설정
- 리셋 정책 설정

---

## 구현 우선순위

### Phase 1: 기본 출석 체크
1. Schedule 타입 및 API
2. Attendance 타입 및 API
3. 관리자: 일정 등록 화면
4. 회원: 출석체크 카드

### Phase 2: 포인트 시스템
1. PointLog 타입 및 API
2. 출석 완료 시 포인트 자동 부여
3. 회원별 포인트 조회
4. 관리자: 포인트 설정

### Phase 3: 레벨 자동화
1. 포인트 기반 레벨 자동 산정
2. 활동지수 탭 개편
3. 레벨별 그룹핑 표시

---

## 출석 → 포인트 연동 로직

### 포인트 부여 시점

**Option A: 일정 마감 시 자동 부여**
- 마감 시간(금요일 자정) 이후 배치 처리
- "참석" 상태인 회원에게만 포인트 부여
- 장점: 자동화, 실제 참석 여부와 무관하게 신청 기준
- 단점: 신청만 하고 실제 불참해도 포인트 받음

**Option B: 관리자 확인 후 부여**
- 토요일 연습 후 관리자가 실제 참석자 확인
- 확인된 참석자에게만 포인트 부여
- 장점: 정확한 참석 기반
- 단점: 관리자 추가 작업 필요

**Option C: 하이브리드 (권장)**
- 기본은 "참석" 신청자에게 자동 부여
- 실제 불참 시 관리자가 포인트 차감/취소 가능
- 대부분의 경우 자동 처리, 예외만 수동

### 미정 상태 처리

- "미정"으로 남긴 경우 → 포인트 미부여
- 마감 전까지 "참석"으로 변경 가능
- 마감 후 변경 불가 (관리자만 수정 가능)

---

## 미결정 사항

1. [ ] 포인트 리셋 정책 (영구 누적 / 연간 리셋 / 롤링)
2. [ ] 레벨 구간 기준 포인트
3. [ ] 스태프 라벨과 활동 레벨 UI 분리 방식
4. [ ] 정기 연습 일정 자동 생성 여부
5. [ ] 포인트 부여 시점 (마감 자동 / 관리자 확인 / 하이브리드)
6. [ ] 출석체크 버튼 UI (바텀시트 vs 인라인 토글)

---

## 참고

- 현재 불참 표시: 카카오톡 캘린더 사용 중
- 목표: 이 앱으로 출석 관리 일원화
- 카톡 단톡방에서는 "출석체크 해주세요" 안내만 하고, 실제 체크는 앱에서

---

## 구현 가이드 (다음 세션용)

### Step 1: 타입 추가 (`src/lib/types.ts`)

```typescript
// 일정 유형
export type ScheduleType = 'regular' | 'event' | 'competition';

// 일정
export interface Schedule {
  id: string;
  type: ScheduleType;
  title: string;
  date: string;              // YYYY-MM-DD
  deadlineDate: string;      // 출석 마감일 YYYY-MM-DD
  deadlineTime: string;      // 출석 마감 시간 HH:mm (예: "23:59")
  points: number;            // 참여 시 부여 포인트
  isActive: boolean;         // 출석 체크 가능 여부
  createdAt: string;
  createdBy: string;
}

// 출석 상태 (카카오톡 캘린더와 동일한 구조)
export type AttendanceStatus = 'attending' | 'not_attending' | 'undecided';

// 출석 기록
export interface Attendance {
  id: string;
  scheduleId: string;
  memberId: string;
  status: AttendanceStatus;
  checkedAt: string;
  updatedAt?: string;        // 상태 변경 시
}

// 포인트 로그
export interface PointLog {
  id: string;
  memberId: string;
  scheduleId?: string;       // 일정 연결 (수동 부여 시 null)
  points: number;
  reason: string;            // "정기 연습 참여", "대회 참여" 등
  createdAt: string;
}

// 포인트 설정 (Settings에 추가)
export interface PointSettings {
  regularPracticePoints: number;   // 정기 연습 (기본 1)
  eventPoints: number;             // 이벤트 (기본 5)
  competitionPoints: number;       // 대회 (기본 10)
  levelThresholds: {
    regular: number;               // 일반 레벨 최소 포인트
    passionate: number;            // 열정 레벨 최소 포인트
    core: number;                  // 핵심 레벨 최소 포인트
  };
}
```

### Step 2: 상수 추가 (`src/lib/constants.ts`)

```typescript
// 일정 유형 라벨
export const SCHEDULE_TYPE_LABELS: Record<ScheduleType, string> = {
  regular: '정기 연습',
  event: '이벤트',
  competition: '대회',
};

// 출석 상태 라벨
export const ATTENDANCE_STATUS_LABELS: Record<AttendanceStatus, string> = {
  attending: '참석',
  not_attending: '불참',
  undecided: '미정',
};

// 출석 상태 색상
export const ATTENDANCE_STATUS_COLORS: Record<AttendanceStatus, string> = {
  attending: 'bg-green-100 text-green-700',
  not_attending: 'bg-red-100 text-red-700',
  undecided: 'bg-gray-100 text-gray-600',
};

// 기본 포인트 설정
export const DEFAULT_POINT_SETTINGS: PointSettings = {
  regularPracticePoints: 1,
  eventPoints: 5,
  competitionPoints: 10,
  levelThresholds: {
    regular: 10,
    passionate: 30,
    core: 60,
  },
};
```

### Step 3: API 함수 추가 (`src/lib/api.ts`)

```typescript
// localStorage 키
const SCHEDULES_KEY = 'zlsu_schedules';
const ATTENDANCES_KEY = 'zlsu_attendances';
const POINT_LOGS_KEY = 'zlsu_point_logs';

// === 일정 관리 ===
export function getSchedules(): Schedule[] { ... }
export function getScheduleById(id: string): Schedule | undefined { ... }
export function createSchedule(schedule: Omit<Schedule, 'id' | 'createdAt'>): Schedule { ... }
export function updateSchedule(id: string, updates: Partial<Schedule>): Schedule | null { ... }
export function deleteSchedule(id: string): boolean { ... }
export function getUpcomingSchedule(): Schedule | null { ... }  // 다가오는 일정 (출석체크 대상)

// === 출석 관리 ===
export function getAttendances(): Attendance[] { ... }
export function getAttendancesBySchedule(scheduleId: string): Attendance[] { ... }
export function getAttendanceByMemberAndSchedule(memberId: string, scheduleId: string): Attendance | undefined { ... }
export function checkAttendance(scheduleId: string, memberId: string, status: AttendanceStatus): Attendance { ... }

// === 포인트 관리 ===
export function getPointLogs(): PointLog[] { ... }
export function getPointLogsByMember(memberId: string): PointLog[] { ... }
export function getMemberTotalPoints(memberId: string): number { ... }  // 총 포인트 계산
export function addPointLog(log: Omit<PointLog, 'id' | 'createdAt'>): PointLog { ... }

// === 포인트 기반 레벨 계산 ===
export function calculateActivityLevel(points: number): ActivityLevel {
  const settings = getSettings();
  const thresholds = settings.pointSettings?.levelThresholds || DEFAULT_POINT_SETTINGS.levelThresholds;

  if (points >= thresholds.core) return 'core';
  if (points >= thresholds.passionate) return 'passionate';
  if (points >= thresholds.regular) return 'regular';
  return 'newbie';
}

// === 출석 마감 처리 (포인트 자동 부여) ===
export function processScheduleCompletion(scheduleId: string): void {
  // 참여로 체크한 회원들에게 포인트 부여
  // PointLog 생성
}
```

### Step 4: 페이지 추가/수정

**새로 만들 파일:**
- `src/pages/admin/SchedulesPage.tsx` - 일정 관리 (관리자)
- `src/pages/SchedulesPage.tsx` - 일정 보기 (회원용)
- `src/components/AttendanceButton.tsx` - 출석체크 버튼 (자주 찾는 메뉴용)
- `src/components/AttendanceModal.tsx` - 출석체크 모달/바텀시트

**수정할 파일:**
- `src/pages/HomePage.tsx` - "자주 찾는 메뉴"에서 수모 구입 → 출석체크 버튼으로 교체
- `src/pages/MembersPage.tsx` - 순위 탭을 포인트 기반으로 변경
- `src/pages/admin/SettingsPage.tsx` - 포인트 설정 추가
- `src/App.tsx` - 라우트 추가
- `src/components/common/Header.tsx` - 일정 메뉴 추가 (선택)

### Step 5: 라우트 추가 (`src/App.tsx`)

```typescript
// 회원 라우트
<Route path="/schedules" element={<SchedulesPage />} />  // 일정 보기

// 관리자 라우트
<Route path="/admin/schedules" element={<AdminSchedulesPage />} />  // 일정 관리
```

### Step 6: 메뉴 추가 (`src/components/common/Header.tsx`)

관리자 메뉴에 "일정 관리" 추가

### Step 7: 홈페이지 수정 (`src/pages/HomePage.tsx`)

**현재 "자주 찾는 메뉴" 섹션 위치:** 약 497번째 줄

**현재 코드 구조 (라인 548-555):**
```tsx
{/* 수모 추가 구입 */}
<Link
  to="/request/swim-cap"
  className="flex flex-col items-center justify-center p-4 bg-blue-50 hover:bg-blue-100 rounded-xl transition-all hover:scale-105"
>
  <span className="text-3xl mb-1">🏊</span>
  <span className="text-sm font-bold text-blue-900">수모 추가 구입 안내</span>
</Link>
```

**변경 내용:**
1. 위 "수모 추가 구입" Link를 출석체크 버튼으로 교체
2. 수모 구입은 GuidePage의 FAQ나 별도 위치로 이동 (또는 제거)

**변경 후 코드 예시:**
```tsx
{/* 이번 주 출석체크 */}
{(() => {
  const upcomingSchedule = getUpcomingSchedule();
  const myAttendance = upcomingSchedule && user
    ? getAttendanceByMemberAndSchedule(user.id, upcomingSchedule.id)
    : null;

  if (!upcomingSchedule) {
    return (
      <div className="flex flex-col items-center justify-center p-4 bg-gray-100 rounded-xl opacity-50">
        <span className="text-3xl mb-1">📅</span>
        <span className="text-sm font-bold text-gray-500">이번 주 일정 없음</span>
      </div>
    );
  }

  const statusColors = {
    attending: 'bg-green-50 hover:bg-green-100 border-green-200',
    not_attending: 'bg-red-50 hover:bg-red-100 border-red-200',
    undecided: 'bg-gray-50 hover:bg-gray-100 border-gray-200',
  };
  const statusLabels = {
    attending: '참석 신청됨',
    not_attending: '불참 신청됨',
    undecided: '미정',
  };
  const currentStatus = myAttendance?.status;

  return (
    <button
      onClick={() => openAttendanceModal(upcomingSchedule)}
      className={`flex flex-col items-center justify-center p-4 rounded-xl transition-all hover:scale-105 border-2 ${
        currentStatus ? statusColors[currentStatus] : 'bg-primary-50 hover:bg-primary-100 border-primary-200'
      }`}
    >
      <span className="text-3xl mb-1">📅</span>
      <span className="text-sm font-bold text-gray-900">
        {currentStatus ? statusLabels[currentStatus] : '출석체크'}
      </span>
      <span className="text-xs text-gray-500">
        {formatShortDate(upcomingSchedule.date)} {upcomingSchedule.title}
      </span>
    </button>
  );
})()}
```

---

## 스태프 라벨 분리 방안

### 현재 문제
`activityLevel: 'staff'`로 되어 있어 역할과 활동량이 혼재

### 해결 방안

**Option A: position 필드 활용 (권장)**
- 이미 `position` 필드 존재 (수영 지도, 수모 담당 등)
- 스태프 여부는 `position`이 있는지로 판단
- `activityLevel`에서 'staff' 제거, 포인트 기반으로만 산정

```typescript
// 스태프 여부 확인
const isStaff = (member: Member) => !!member.position;

// 활동 레벨은 포인트로만 계산 (newbie, regular, passionate, core)
// UI에서 스태프는 별도 뱃지로 표시
```

**Option B: 별도 필드 추가**
```typescript
interface Member {
  isStaff?: boolean;        // 스태프 여부 (새 필드)
  activityLevel?: ActivityLevel;  // 포인트 기반 (staff 제외)
}
```

### UI 표시 예시
```
🏆 한선우 (수영 지도)     // 스태프 뱃지 + position
   45점 · 열정 레벨       // 포인트 + 활동 레벨
```

---

## 마이그레이션 계획

기존 데이터와의 호환성:

1. `activityLevel: 'staff'`인 회원 → `position` 확인 후 적절한 포인트 레벨로 변환
2. 기존 회원들 초기 포인트 부여 방안 결정 필요
   - 옵션 A: 모두 0점에서 시작
   - 옵션 B: 가입 기간 기반 초기 포인트 부여
   - 옵션 C: 현재 레벨 기반 초기 포인트 부여

---

## 체크리스트

구현 시작 전 확인:
- [ ] 포인트 리셋 정책 결정
- [ ] 레벨 구간 포인트 확정
- [ ] 스태프 분리 방안 확정 (Option A vs B)
- [ ] 기존 회원 초기 포인트 정책 결정
- [ ] 정기 연습 일정 자동 생성 여부 결정
